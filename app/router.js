module.exports = app => {
    const { router, controller } = app;
    // router.get("/activityPage",  controller.home.index);
    // router.get("/login",  controller.home.index);
    // router.get("/departmentManage/*",controller.home.index)
};
